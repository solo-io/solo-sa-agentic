apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: demo-anthropic-route
  namespace: agentgateway-system
spec:
  hostnames:
  - demo-anthropic.example.com
  parentRefs:
  - name: https
  rules:
  - backendRefs:
    - group: agentgateway.dev
      kind: AgentgatewayBackend
      name: anthropic-llm
