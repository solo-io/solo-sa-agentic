apiVersion: v1
kind: Service
metadata:
  name: demo-mcp
  namespace: agentgateway-system
  labels:
    app.kubernetes.io/name: demo-mcp
spec:
  selector:
    app.kubernetes.io/name: demo-mcp
  ports:
  - name: http
    port: 8080
    targetPort: http
