apiVersion: v1
kind: Service
metadata:
  name: core-banking-mcp
  namespace: agentgateway-system
  labels:
    app.kubernetes.io/name: core-banking-mcp
spec:
  selector:
    app.kubernetes.io/name: core-banking-mcp
  ports:
  - name: http
    port: 8080
    targetPort: http
