apiVersion: v1
kind: Service
metadata:
  name: mock-llm
  namespace: agentgateway-system
  labels:
    app.kubernetes.io/name: mock-llm
spec:
  selector:
    app.kubernetes.io/name: mock-llm
  ports:
  - name: http
    port: 80
    targetPort: http
