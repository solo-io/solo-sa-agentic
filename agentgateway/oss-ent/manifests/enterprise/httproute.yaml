apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mock-llm-enterprise
spec:
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: enterprise-agentgateway-proxy
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - group: agentgateway.dev
      kind: AgentgatewayBackend
      name: mock-llm-enterprise
